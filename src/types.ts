export type dummyMeals = {
  title: string;
  slug: string;
  image: string;
  summary: string;
  instructions: string;
  creator: string;
  creator_email: string;
}[];

export type mealItem = {
  title: string;
  slug: string;
  image: string;
  summary: string;
  creator: string;
};
